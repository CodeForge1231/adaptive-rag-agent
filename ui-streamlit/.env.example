# Base URL of the running Adaptive RAG Agent API
BASE_API_URL=http://localhost:8000

# Security token for API authorization (must match API_TOKEN in the agent's .env)
API_TOKEN=your-secret-token-here
